/** @import {UserConfig} from './config.js' */

/**
 * Function wich define config for builder
 *
 * @param {UserConfig} config
 * @returns {UserConfig}
 */
export function defineConfig(config) {
	return config;
}
